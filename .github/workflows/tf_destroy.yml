name: Destroy Infrastructure

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  CLUSTER_NAME: payment-dev-eks-cluster
  TF_VERSION: 1.15.4

jobs:
  destroy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'prod' || github.ref_name == 'develop' && 'dev'}}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine Environment
        id: env
        run: |
          if [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "env=prod" >> $GITHUB_OUTPUT
          elif [[ "${GITHUB_REF_NAME}" == "develop" ]]; then
            echo "env=dev" >> $GITHUB_OUTPUT
          else
            echo "env=dev" >> $GITHUB_OUTPUT
          fi

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_ROLE }}
      - name: Configure Kubeconfig
        run: |
         aws eks update-kubeconfig \
          --region $AWS_REGION \
          --name $CLUSTER_NAME

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Delete Ingress
        run: |
         kubectl delete -f kubernetes/ingress \
           --ignore-not-found=true

      - name: Delete Application
        run: |
          kubectl delete -f kubernetes/apps \
            --ignore-not-found=true
            
      - name: Wait for ELB Cleanup
        run: |
          while [ $(aws elbv2 describe-load-balancers --query 'length(LoadBalancers)' --output text) -gt 0 ]
          do
            echo "Waiting..."
            sleep 30
          done

      - name: Remove Addons
        run: |
          helm uninstall aws-load-balancer-controller -n kube-system || true
          helm uninstall ingress-nginx -n ingress-nginx || true
          helm uninstall monitoring -n monitoring || true
          helm uninstall argocd -n argocd || true
          helm uninstall cert-manager -n cert-manager || true

      - name: Terraform Init
        run: terraform init

      - name: Terraform Apply
        run: terraform destroy -auto-approve